export const metadata = {
  title: "Users Admin | Kevixo",
  robots: {
    index: false,
    follow: false,
  },
};

export default function UsersAdminLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
